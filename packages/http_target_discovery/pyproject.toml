[project]
name = "http_target_discovery"
version = "1.0.0"
description = ""
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "docker>=7.1.0",
    "apscheduler>=3.10,<4",
    "kubernetes>=36.0.2",
    "loguru>=0.7.3",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.2",
    "requests>=2.32.3",
]

[project.scripts]
http_target_discovery = "http_target_discovery.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
