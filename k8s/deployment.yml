apiVersion: apps/v1
kind: Deployment
metadata:
  name: dummy
spec:
  replicas: 0
  selector:
    matchLabels:
      app: dummy
  template:
    metadata:
      labels:
        app: dummy
    spec:
      containers:
        - name: dummy
          image: nginx:latest
          ports:
            - containerPort: 80
